import React from "react";

// Fixed full-screen SVG noise overlay for the "realistic / film" feel.
export const GrainOverlay = () => (
    <div className="grain-overlay" aria-hidden="true" data-testid="grain-overlay" />
);

export default GrainOverlay;
